// Audit consumer — reads from audit.events Kafka topic, writes to S3 Object
// Lock (compliance mode) for FCA/EU regulatory retention.
// Adapter interface: writeEvent, queryByTimeRange, queryByEntityId.
// Implementation: phase 5 (see CLAUDE.md implementation order).

console.log('audit-consumer: not yet implemented');
